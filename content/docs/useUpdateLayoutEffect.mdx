---
title: useUpdateLayoutEffect
description: A hook alike useLayoutEffect but skips running the effect for the first time.
---

## Overview

A hook alike `useLayoutEffect` but skips running the effect for the first time.

[Documentation and Examples](https://ahooks.js.org/hooks/use-updatelayouteffect)

## Installation

<OpenInV0Button url="https://shadcn-ahooks.vercel.app/r/useUpdateLayoutEffect.json" />

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
  ```bash
  pnpm dlx shadcn@latest add https://shadcn-ahooks.vercel.app/r/useUpdateLayoutEffect.json
  ```
  </Tab>
  <Tab value="npm">
  ```bash
  npx shadcn@latest add https://shadcn-ahooks.vercel.app/r/useUpdateLayoutEffect.json
  ```
  </Tab>
  <Tab value="yarn">
  ```bash
  yarn shadcn@latest add https://shadcn-ahooks.vercel.app/r/useUpdateLayoutEffect.json
  ```
  </Tab>
  <Tab value="bun">
  ```bash
  bun shadcn@latest add https://shadcn-ahooks.vercel.app/r/useUpdateLayoutEffect.json
  ```
  </Tab>
</Tabs>

A hook alike `useLayoutEffect` but skips running the effect for the first time.

## Examples

### Basic usage

<Preview name="useUpdateLayoutEffect"
 code={`
import useUpdateLayoutEffect from '@/src/hooks/ahooks/useUpdateLayoutEffect';
import React, { useLayoutEffect, useState } from 'react';

const Example = () => {
  const [count, setCount] = useState(0);
  const [layoutEffectCount, setLayoutEffectCount] = useState(0);
  const [updateLayoutEffectCount, setUpdateLayoutEffectCount] = useState(0);

  useLayoutEffect(() => {
    setLayoutEffectCount((c) => c + 1);
  }, [count]);

  useUpdateLayoutEffect(() => {
    setUpdateLayoutEffectCount((c) => c + 1);
    return () => {
      // do something
    };
  }, [count]); // you can include deps array if necessary

  return (
    <div>
      <p>layoutEffectCount: {layoutEffectCount}</p>
      <p>updateLayoutEffectCount: {updateLayoutEffectCount}</p>
      <p>
        <button type="button" onClick={() => setCount((c) => c + 1)}>
          reRender
        </button>
      </p>
    </div>
  );
};

export default Example;`} />

## API

The API is exactly the same as `React.useLayoutEffect`.

```typescript
useUpdateEffect(
  effect: React.EffectCallback,
  deps?: React.DependencyList,
)
```
